FROM node:22-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends bash ca-certificates curl git jq python3 procps tmux docker.io docker-compose \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /root
CMD ["sleep", "infinity"]
